<!DOCTYPE html>
<html>
<head>
	<title>transition</title>
</head>
<body>
	<p>click to confirm documentation of information</p>
	<div class="hold">
		<h1>connected users id</h1>
		<div class="online"></div>
	</div>
	<style type="text/css">
		*
		{
			margin: 0;
			padding: 0;
			box-sizing: border-box;
		}
		body
		{
			height: 100vh;
			width: 100vw;
			display: flex;
			flex-direction: column;
			justify-content: space-around;
			align-items: center;
			background: #1d1d1d;
			color: #fff;

		}
		h1
		{
			color: #fff;
			font-family: tahoma,sans serif;
			font-weight: 700;
		}
		.hold
		{
			width: 100%;
			min-height: 200px;
			max-width: 300px;
			display: flex;
			flex-direction: column;
			justify-content: space-between;
			align-items: center;
			grid-row-gap: 10px;
		}
		.online
		{
			display: flex;
			justify-content: space-around;
			background: green;
			align-items: center;
			flex-direction: column-reverse;
			color: cyan;
			font-family: cursive;
			font-weight: 900;
			font-size: 17px;
			grid-column-gap: 10px;
			flex-wrap: wrap;
		}
		.p
		{
			background: #171717;
		}
	</style>
	<script type="text/javascript">
		var onl = document.querySelector('.online');
		let iterable = '<%= online %>' 
		// (iterable.split(','));
		for(i = 0; i<iterable.split(',').length;i++)
		{
			let p = document.createElement('p');
			p.innerHTML = iterable.split(',')[i];
			p.classList.add('p');
			onl.appendChild(p);
		}
	</script>

</body>
</html>